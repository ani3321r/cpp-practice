#include<iostream>
using namespace std;

int main(){
    unsigned int x = 8;
    unsigned int y = 3;// we mainly use unsigned int for bitwise operations

    unsigned int z;
    /*
    z = x & y /bitwise and
    z = x | y /bitwise or
    z = x ^ y /bitwise XOR
    z = ~x /not op
    z = y << 1 /left shift of 1
    z = y >> 1 /right shift of 1
    */
}